/**
 * UniBus Vendor Dashboard — Central Configuration
 * =====================================================
 * সার্ভার বা Firebase URL পরিবর্তন করতে হলে শুধু এই ফাইলটি এডিট করুন।
 *
 * Local Development:
 *   API_BASE = 'http://127.0.0.1:8000/api'
 *
 * Production (Render / Railway / VPS):
 *   API_BASE = 'https://your-app.onrender.com/api'
 */

const CONFIG = {
  // ⚙️ শুধু এই লাইনটি পরিবর্তন করুন:
  API_BASE: 'https://unibus-backend-z8e6.onrender.com/api',

  // 🔥 Firebase Realtime Database URL
  FIREBASE_DB_URL: 'https://unibus-app-e8e4b-default-rtdb.firebaseio.com',
};
